# VITAL ID Demo

## Run locally
```bash
npm install
npm run dev
```
Open http://localhost:3000

## Deploy to Vercel
Framework preset: **Next.js**. Root directory: repository root (the folder containing `package.json`).
